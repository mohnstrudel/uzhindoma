%main.page-content
	%section.bg-light.section-lg
		%ol.breadcrumb
			%li
				%a{:href => "index.html"} Home
			%li
				%a{:href => "#"} Elements
			%li.active Tabs & Accordions
	-@current_menus.each do |cm|
		=link_to cm.category.name, menu_path(cm.id), remote: true, class: "btn btn-success-mod-1 btn-sm"
	.section#render_menus
		=render 'front/menus/show_menu'