%main.page-content
	%section.bg-light.section-lg
		%ol.breadcrumb
			%li
				%a{:href => "index.html"} Home
			%li
				%a{:href => "#"} Elements
			%li.active Tabs & Accordions
	-if @current_menus.empty?
		%section.section.section-inset-1
			.container
				%h2.text-center Актуальное меню
				%hr
				.col-xs-12.col-sm-12.col-lg-12
					.thumbnail.thumbnail-mod-1
						%span.icon.icon-success.icon-lg.icon-mod-1.fa-desktop
						.caption
							%h4
								Извините!
							%p Что-то пошло не так и меню текущей неделе потерялось на нашей кухне. Мы очень быстро это поправим, обещаем :)
	-else
		-@current_menus.each do |cm|
			=link_to cm.category.name, menu_path(cm.id), remote: true, class: "btn btn-success-mod-1 btn-sm"
		.section#render_menus
			=render 'front/menus/show_menu'